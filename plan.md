# 需求

## 一、项目定位

是一个用于管理、部署和运行Minecraft服务端的 Agent 平台，旨在让不懂技术的人也可以通过简单的上传服务端即可部署服务端。
系统需要支持服务端文件管理、日志交互、Agent 辅助部署、多服务端切换、模型配置、安全沙箱等能力。
此项目仅供内部使用，有简单密钥验证身份即可，不需要考虑任何安全问题。

---

# 二、核心功能需求

## 1. 服务端运行与交互

### 1.1 服务端日志窗口

系统需要提供一个窗口，用于实时显示当前运行服务端的日志。

要求：

* 可以实时查看服务端输出日志。
* 支持滚动查看历史日志。
* 支持在日志窗口中输入指令，与服务端进行交互。
* 输入的指令需要发送到当前正在运行的服务端控制台。

### 1.2 服务端启动、关闭与重启

前端需要支持对服务端进行管理。

要求：

* 支持启动服务端。
* 支持正常关闭服务端。
* 支持强制关闭服务端。
* 支持重启服务端。
* 需要显示当前服务端运行状态，例如：

  * 未启动
  * 启动中
  * 运行中
  * 关闭中
  * 已停止

---

## 2. 多服务端管理

### 2.1 支持多个服务端配置

系统需要支持管理多个服务端。

要求：

* 用户可以创建多个服务端配置。
* 新创建服务端后（服务端内容为空），应该只显示和Agent对话的界面，并附上简短说明。
* 每个服务端应有独立的目录、配置和运行参数。
* 可以切换当前选中的服务端。
* 切换使用左侧折叠列表进行。
* 不同的服务端使用不同的 Agent 对话上下文。

### 2.2 同时只能运行一个服务端

虽然系统支持多个服务端配置，但同一时间只能启动一个服务端。

要求：

* 如果已有服务端正在运行，启动另一个服务端前需要先关闭当前服务端。
* 前端需要提示用户当前已有服务端正在运行。
* Agent 和后端都需要遵守该限制，避免同时启动多个服务端。

---

## 3. 文件管理页面

系统需要提供文件管理页面，用于管理服务端相关文件。

要求：

* 浏览服务端目录结构。
* 查看文件和文件夹。
* 上传文件。
* 下载文件。
* 新建文件夹。
* 删除文件或文件夹。
* 重命名文件或文件夹。
* 编辑文本文件，例如：

  * `.json`
  * `.yml`
  * `.properties`
  * `.txt`
  * `.cfg`
* 文件操作必须限制在服务端专属目录内。

---

## 4. Agent 交互窗口

系统需要提供一个窗口用于用户和 Agent 对话。

要求：

* 用户可以通过前端向 Agent 发送任务。
* Agent 可以返回文本、操作结果、错误信息和执行日志。
* Agent 可以根据用户请求执行服务端部署、文件修改、配置检查等操作。
* Agent 的操作需要受到权限和目录限制。
* 前端需要能显示 Agent 当前任务状态，例如：

  * 空闲
  * 思考中
  * 执行中
  * 等待用户确认
  * 执行完成
  * 执行失败

---

# 三、Agent 能力需求

## 1. 新增服务端能力

Agent 需要支持帮助用户新增服务端。

要求：

* Agent 可以根据模板创建服务端目录。
* Agent 可以生成基础配置文件。
* Agent 可以引导用户补充必要信息。
* Agent 创建的所有文件必须放在指定的服务端目录下。

---

## 2. Java 环境选择

Agent 需要能够选择正确的 Java 版本。

要求：

* Agent 能够根据服务端或整合包要求选择合适的 Java。
* 如果缺少所需 Java 版本，需要安装相应的 Java 版本在工作软件的工作环境中，供其他服务端使用。
* 不要使用系统自带 Java。
* 服务端配置中需要保存对应 Java 路径。
* 启动服务端时使用配置中指定的 Java。

---

## 3. 按模板构建服务器

Agent 需要支持按照固定模板构建服务端。
可参考的模板在 "D:\Desktop\1 (1)"

要求：

* 系统内置服务端构建模板。
* Agent 根据模板将用户要求的整合包服务端融合进模板中。

---

## 4. 查询整合包相关信息

Agent 的System Prompt 需要查询整合包相关信息。

要求：

* Agent 可以根据整合包名称、版本或用户上传的文件查询相关信息。
* 需要查询的信息包括：

  * 服务端的部署方法
  * 整合包适配的 Minecraft 版本
  * 需要的 Java 版本
  * 常见启动参数
  * 常见报错解决方案
* 查询结果需要整理后展示给用户。

---

## 5. Skill 支持

系统需要支持 Skill 机制。

要求：

* Codex / OpenAI Skill 的目录规范
* Agent 可以加载和使用 Skill。
* Skill 可以扩展 Agent 的能力。
* 系统需要内置一个“服务端部署 Skill”。
* 服务端部署 Skill 应包含：

  * 服务端初始化
  * Java 检查
  * 模板生成
  * 配置修改
  * 启动脚本生成
  * 常见问题检查

---

# 四、模型与 Prompt 配置

## 1. 前端管理模型和 API Key

前端需要支持管理 Agent 使用的模型和 API Key。

要求：

* 可以新增模型配置。
* 可以修改模型名称。
* 可以修改 Base URL。
* 可以修改 API Key，应该只能覆盖编辑不能查看。
* 可以测试模型连通性。
* API Key 不应明文展示。
* 后端需要安全存储 API Key。

---

## 2. 前端修改默认 System Prompt

前端需要支持修改 Agent 的默认 System Prompt。

要求：

* 用户可以查看当前默认 System Prompt。
* 用户可以编辑并保存默认 System Prompt。
* 支持恢复默认 Prompt。
* 不同服务端可以使用不同 Prompt，也可以使用全局默认 Prompt。
* 修改后需要影响后续 Agent 对话。

---

# 五、文件上传需求

## 1. 用户上传文件给 Agent

系统需要支持用户上传文件给 Agent 使用。

要求：

* 用户可以在 Agent 对话窗口上传文件。
* 上传的文件存放在临时目录中。
* Agent 可以读取临时目录中的文件。
* 临时文件需要和正式服务端文件隔离。
* 用户可以选择是否将临时文件移动到服务端目录。
* 临时文件应支持自动清理。

临时目录示例：

```text
workspace/
  temp_uploads/
    session_xxx/
      uploaded_file.zip
```

---

# 六、安全需求

## 1. 服务端文件隔离

所有服务端文件必须单独存储在一个固定目录下。

示例：

```text
workspace/
  servers/
    server_001/
    server_002/
    server_003/
```

要求：

* Agent 只能访问 `workspace/servers/` 下的内容。
* Agent 不能修改系统其他目录。
* 文件管理页面也只能操作服务端目录。
* 后端必须防止路径穿越攻击，例如禁止访问：

---

# 七、推荐页面结构

前端可以分为以下几个主要页面：

```text
1. 首页
   - 显示若干卡片

2. 侧边栏
   - 显示所有服务端，点击切换服务端
   - 左下角显示时间和设置按钮
3. 控制台卡片
   - 实时日志
   - 指令输入
   - 启动 / 关闭 / 强制关闭 / 重启

4. 文件管理卡片
   - 文件浏览
   - 上传 / 下载
   - 编辑 / 删除 / 重命名

5. Agent 卡片
   - 和 Agent 对话
   - 上传文件
   - 查看 Agent 执行过程

6. 模型配置（设置中）
   - 模型管理
   - API Key 管理
   - Base URL 配置
   - 测试连接

7. Prompt 设置（设置中）
   - 默认 System Prompt
   - 服务端专属 Prompt
   - 恢复默认配置

8. Skill 管理页面（设置中）
   - 查看已安装 Skill
   - 启用 / 禁用 Skill
   - 内置服务端部署 Skill
```

---

# 八、系统角色划分

## 前端

使用：

```text
Nuxt + Vue
```

负责：

* 页面展示
* 用户交互
* 服务端列表管理
* 控制台日志展示
* 文件管理 UI
* Agent 对话 UI
* 模型配置 UI
* Prompt 配置 UI

---

## 后端

使用：

```text
Node.js + TypeScript + Fastify
```

负责：

* 服务端进程管理
* 文件系统操作
* 日志流转发
* 指令输入转发
* API Key 存储
* 权限控制
* 和 Agent 通信

实现要求：

* 使用 WebSocket 转发服务端日志、控制台指令和 Agent 执行状态。
* 使用 `child_process.spawn` 管理 Minecraft 服务端进程。
* 使用 `fs/promises` 进行文件操作，并通过路径沙箱限制访问范围。
* 使用 SQLite + Drizzle ORM 存储服务端配置、模型配置、Prompt 和 Agent 会话。
* 使用 `node:crypto` 加密存储 API Key。
* Agent 通过 LangGraph.js 调用后端受控工具，不直接访问任意系统路径或执行任意命令。

---

## Agent

使用：

```text
LangGraph
```

负责：

* 理解用户需求
* 调用工具
* 新建服务端
* 查询整合包信息
* 检查 Java 环境
* 根据模板构建服务器
* 修改配置文件
* 调用 Skill

---
# 技术栈

LangGraph.js + Node.js + Nuxt + Vue

使用LangGraph 驱动 Agent
使用 Nuxt + Vue 完成前端业务
使用 Node.js + TypeScript + Fastify 完成后端业务
